import React from 'react'

const EjemploPropChildren = (props) => {
  
    return (
    <div>
        <h1>Aca mostramos las prop children</h1>    
        {props.children}
    </div>
  )

}

export default EjemploPropChildren